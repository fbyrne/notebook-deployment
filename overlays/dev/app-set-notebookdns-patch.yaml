kind: Deployment
metadata:
  name: notes-app
spec:
  template:
    spec:
      containers:
        - name: notes-app
          env:
            - name: NOTEBOOK_INGRESS_DNS
              value: "http://$(NOTEBOOK_INGRESS_DNS)"